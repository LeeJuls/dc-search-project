# 프로젝트 히스토리 (전체 코드 리뷰 및 버그 수정)
**버전:** v2.0
**날짜:** 2026-03-19 ~ 2026-03-29

## 1. 배경 및 발단

라이브 서버(Render)에서 대시보드 접속 시 "데이터를 수집 및 집계 중입니다." 메시지만 표시되는 문제가 지속되었다.
오너의 전체 코드 리뷰 요청으로 원인 진단 및 다수의 버그 수정 작업을 진행하였다.

---

## 2. 주요 의사 결정 사항

### (1) 감성 사전 방향 전환
욕설 포함 여부 → **게임 플레이 경험 기반** 키워드로 전환하기로 합의.
- "욕설이 들어가도 좋은 반응일 수 있고, 욕설 없어도 부정일 수 있다"는 커뮤니티 맥락 반영
- "사기캐", "밸붕", "갓겜", "혜자", "무과금" 등 게임 유저 고유 어휘 중심으로 재구성
- LLM 프롬프트에 욕설 판단 금지 가이드라인 명시

### (2) 키워드 매칭 우선순위 정책
"무과금 ㅈ망겜" 같은 표현에서 "과금"이 먼저 매칭되어 긍정/부정 혼동 발생 사례를 오너가 제기.
→ **긴 키워드 우선 매칭** 정책 채택 (예: "무과금" > "과금" 순으로 탐색)

### (3) Supabase 1000행 한계
오너 질문으로 발견. Supabase 기본 페이지네이션이 1000행 제한.
→ 모든 쿼리에 `.limit(5000)` 명시 적용으로 단기 대응.
→ 장기적으로 데이터가 5000행 초과 시 페이지네이션 구현 필요 (향후 과제)

### (4) 날짜 필터 방식
"1일" 조회 시 오전 시간대에는 전날 오전 게시글이 누락됨을 발견.
→ 롤링 24시간 방식 → **자정(00:00) 기준** 방식으로 변경 합의

---

## 3. 작업 타임라인

| 커밋 | 내용 |
|------|------|
| `0d48a36` | 감성 사전 게임 키워드 추가, KST 시간대 수정, 갤러리 링크 아이콘 |
| `39bb889` | 구글 시트 기반 게임 유저 감성 사전 전면 개편 |
| `f3f6617` | 감성 분석 오매칭 방지 - 긴 키워드 우선 매칭 로직 적용 |
| `4311fee` | ReportGenerator 초기화 시 db.db_name 속성 참조 오류 수정 |
| `6039a82` | KST 시간대 전면 적용, 하드코딩 제거, 레거시 정리 |
| `859e21e` | 감성 분석 upsert→update 변경으로 NOT NULL 제약 오류 해결 |
| `04cbccb` | 날짜 필터를 롤링 24시간에서 자정 기준으로 변경 |

---

## 4. 발견된 인프라 이슈

### Supabase 이중 프로젝트 혼동
- Supabase에 구글 계정(Google login)과 깃허브 계정(GitHub login) 각각으로 프로젝트가 생성되어 있었음
- 오너가 대시보드에서 확인하던 프로젝트와 `.env`의 프로젝트가 다를 수 있다는 의심이 제기됨
- 확인 결과: `.env`의 `SUPABASE_URL=https://iwyadjubhywkbzgtrlwr.supabase.co`가 실제 데이터가 있는 정확한 프로젝트를 가리키고 있음을 확인
- Supabase 에디터에서 데이터 정상 확인, Render 환경변수도 동일 프로젝트로 설정됨 확인

### GitHub Actions 크롤링 실패
- GitHub Actions ubuntu-latest 러너에서 DC Inside 크롤링 시 `ConnectTimeoutError` 지속 발생
- DC Inside가 클라우드 서버 IP 차단하는 것으로 추정 (방어 정책)
- OpenRouter 무료 모델 429 rate limit도 간헐적 발생
- 크롤링 실패해도 기존 DB 데이터로 대시보드는 정상 서비스됨

---

---

## 5. 추가 작업 (2026-03-20)

### (1) 크롤링/LLM 분리 스케줄링
- 기존 30분 통합 실행 → **크롤링 15분 / LLM 6시간** 분리
- `main.py`에 `skip_llm`, `llm_only` 파라미터 추가
- `scheduler.py`에 환경변수(`SKIP_LLM`, `LLM_ONLY`) 기반 모드 전환
- GitHub Actions 워크플로우 2개 분리: `scraper.yml`(15분), `llm-update.yml`(6시간)
- 대시보드에 업데이트 남은시간 카운트다운 타이머 추가

### (2) KST timezone-aware vs pandas datetime64 TypeError 수정
- `datetime.now(KST)` 적용 후 pandas 날짜 비교에서 `TypeError: Invalid comparison between dtype=datetime64[us] and datetime` 발생
- GitHub Actions에서 크롤링이 매번 실패하고 있었으나, 에러가 `except`에 잡혀 `success`로 리포트됨
- 수정: `target_date.replace(tzinfo=None)`으로 timezone-naive 비교로 통일

### (3) 날짜 쿼리 isoformat 되돌림
- DB 데이터가 KST 시간을 UTC 라벨(+00:00)로 저장하는 구조
- `isoformat()` 사용 시 +09:00이 붙어 9시간 차이 발생, 이전 날짜 게시글이 포함됨
- `strftime('%Y-%m-%d %H:%M:%S')`로 되돌려 KST-as-UTC 데이터 모델과 일치시킴

### (4) Windows cp949 인코딩 호환
- `main.py`의 em dash(U+2014) 문자가 Windows 콘솔(cp949)에서 인코딩 에러 발생
- em dash를 일반 dash(-)로 교체

### (5) Render Sleep 방지
- Render 무료 티어 15분 비활성 시 sleep → UptimeRobot 무료 서비스로 5분 간격 핑 설정

| 커밋 | 내용 |
|------|------|
| `15b4fa4` | 크롤링/LLM 분리 스케줄링 + 업데이트 남은시간 카운트다운 |
| `60d8482` | 날짜 쿼리에서 KST 타임존 정보 누락 버그 수정 |
| `90bbef9` | 날짜 쿼리 isoformat 되돌림 (KST-as-UTC 구조) |
| `9932e08` | KST timezone-aware vs pandas datetime64 비교 TypeError 수정 |
| `442a2fd` | Windows cp949 인코딩 호환 (em dash 제거) |

---

## 6. 추가 작업 (2026-03-25)

### (1) 감성 분석 부정어 처리 개선
- "기대가 안되네" 같은 부정 표현이 긍정으로 오분류되는 버그 수정
- `NEGATION_WORDS` 클래스 변수 추가 (15개 부정어 패턴)
- `analyze_locally()`에서 앞뒤 6글자 window 부정어 탐지 시 점수 반전
- 복합 부정 표현 11개 사전 직접 등록 (재미없다, 기대안됨 등)
- 별도 상세 문서: `docs/dev/감성분석_부정어처리개선_명세서_v1.0_20260325.md`

### (2) 재분석 페이지네이션 개선
- offset 기반 1000개 단위 배치 처리로 5000행 제한 해소
- Supabase 쿼리 안정성 향상

### (3) Docker 배포 지원
- `Dockerfile`, `.dockerignore` 신규 추가
- Render에서 Docker 기반 배포 서비스로 전환
- `days` 파라미터 방어 로직 추가 (ValueError/TypeError 예외 처리)

### (4) 감성 분석 과다 API 호출 수정
- 기존: 매 사이클마다 전체 게시글 재분석 → 일 약 3,175,680건 API 호출
- 수정: lexicon 변경 시에만 force 재분석, 평시에는 미분석 건만 처리 → 일 약 2,880건
- `lexicon_changed` 플래그 기반 조건부 `force=True` 적용

### (5) 재분석 RPC 벌크 업데이트 적용
- 개별 update 호출(11,000건) → Supabase RPC 벌크 업데이트(22회)로 최적화
- API 호출 수 500배 감소

| 커밋 | 내용 |
|------|------|
| `e0bc766` | 감성 분석 부정어 처리 개선 - 긍정/부정 오분류 수정 |
| `c160674` | 재분석 페이지네이션 개선 - offset 기반 1000개 단위 배치 처리 |
| `a5ba958` | Docker 배포 지원 - Dockerfile, .dockerignore 추가 |
| `77a65f8` | 감성 분석 과다 API 호출 수정 - 일 3,175,680건 → 2,880건 |
| `ae275e9` | 재분석 RPC 벌크 업데이트 적용 - API 호출 11,000→22회 |

---

## 7. 추가 작업 (2026-03-29)

### (1) Supabase max_rows=1000 페이지네이션 대응
- `.limit(5000)` 방식이 서버측 `max_rows=1000` 설정을 우회하지 못하는 것을 로컬 디버깅으로 발견
- ovensmash 1일 데이터: 실제 1585건인데 1000건만 반환 → `.range()` 기반 페이지네이션으로 전체 조회

### (2) 인메모리 캐싱 도입
- 1일/7일, 갤러리 전환 시 매번 Supabase 호출 → 3초+ 지연
- `threading.Lock` + `time.monotonic()` 기반 double-check locking 캐시 구현
- 감성 데이터 TTL 10분, 트렌드 데이터 TTL 30분
- 캐시 히트 시 0ms 응답, 에러 응답은 캐싱하지 않음

### (3) 일별 여론 추이 그래프 추가
- Supabase에 `daily_stats` 테이블 신규 생성 (PK: gallery_id + stat_date)
- `backfill_daily_stats` RPC로 기존 posts 데이터 일괄 백필
- 크롤링 사이클마다 당일 통계 자동 집계 (`main.py`)
- Chart.js 4.x CDN 혼합 차트: 막대(게시글 수) + 라인(긍정/부정 비율)
- X축 `MM/DD(요일)` 포맷, Y보조축 동적 범위 (데이터 max + 5%p)
- 1일/7일 뷰 모두에서 14일간 트렌드 표시
- 트렌드 데이터 30분 TTL 캐싱, 빈 날짜 0으로 채우기

| 커밋 | 내용 |
|------|------|
| `739e277` | Supabase max_rows=1000 제한 대응 - 페이지네이션 조회로 전체 데이터 반환 |
| `0fb2971` | 인메모리 캐싱 도입 - Supabase 반복 호출 제거 (TTL 10분) |
| `73f999f` | 일별 여론 추이 그래프 추가 - Chart.js 혼합 차트 (14일간) |

---

## 8. 미해결 및 향후 과제

| 항목 | 설명 | 우선순위 | 상태 |
|------|------|---------|------|
| GitHub Actions 크롤링 실패 | DC Inside 클라우드 IP 차단 → TypeError 수정으로 일부 해결 | 높음 | 부분 해결 |
| 부정어 window 오반전 | "안 써도 됨" 같은 복합 문맥에서 오반전 가능성 | 중간 | 모니터링 중 |
| DB 날짜 모델 정리 | KST-as-UTC 저장 방식의 근본적 한계, 타임존 명시 전환 검토 | 낮음 | 미착수 |
